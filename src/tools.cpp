#include "tools.hpp"

bool PathTool::getPublicPathPart(std::vector<std::filesystem::path> & a)
{
    std::string Pre=a[0].string();
    for (auto it : a) {
        it.string();
    
    }

    return  true;
}